你是专业的跨系统任务聚类助手。下面给出若干编号及其任务名称，请判断哪些编号属于同一事件，并为每个事件生成简洁、专业的任务名（例如：代码提交、代码下载、编译构建、合入发布等）。

## 编号与任务名称
{% for idx, name in numbered_items %}
{{ idx }}: {{ name }}
{% endfor %}

## 要求
1. 合并表示同一事件的编号。
2. 为每个聚合后的事件给出一个简洁的中文任务名，避免冗长表述。
3. 仅返回 JSON 代码块，格式如下：
```json
{
  "任务名A": [1, 2],
  "任务名B": [3]
}
```
请务必只输出上述 JSON 代码块，不要加入额外说明。

